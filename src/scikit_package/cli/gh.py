def _get_broadcast_urls():
    """Get repo urls to broadcast from the interactions with users.

    This function interacts with the user in the following order:
    1. Prompt for the location of files used to decipher the alias for
    the target broadcast repo urls.
    2. Print the recognizable name for each single repo and each
    group of repos.
    3. Prompt to let user type the name of target repos or groups of
    repos to broadcast the issue.

    Returns
    -------
    broadcast_urls : list of str
        a list of repo urls to broadcast the issue.
    """
    broadcast_urls = []
    return broadcast_urls
